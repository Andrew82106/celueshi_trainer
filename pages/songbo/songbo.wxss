/* pages/songbo/songbo.wxss */
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  position: relative;
  background-color: #F8F4FB; /* 稍微偏紫色的背景 */
}

/* 按钮容器样式 */
.settings-container {
  position: absolute;
  top: 130rpx;
  left: 30rpx;
  display: flex;
  flex-direction: row;
  align-items: center;
  z-index: 10;
}

/* 每个按钮的样式 */
.settings-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-right: 40rpx;  /* 按钮之间的间距 */
}

.settings-icon {
  width: 30px;
  height: 30px;
  margin-bottom: 5px;
}

/* 计数器样式 */
.counter {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 40px;
}

.count-text {
  font-size: 60px;
  font-weight: bold;
  color: #7D6B91; /* 紫色调 */
}

.merit-text {
  font-size: 16px;
  color: #666;
  margin-bottom: 5px;
}

.total-count {
  font-size: 14px;
  color: #999;
}

/* 颂钵样式 */
.songbo-container {
  width: 250px;
  height: 250px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.songbo-image {
  width: 100%;
  height: 100%;
  transition: transform 0.1s;
}

/* 颂钵敲击动画 */
.tap-animation {
  transform: scale(0.95);
}

/* 模态框样式 */
.modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 100;
}

.modal-content {
  width: 80%;
  background-color: #fff;
  border-radius: 12px;
  overflow: hidden;
}

.modal-header {
  padding: 20px;
  text-align: center;
  border-bottom: 1px solid #eee;
}

.modal-title {
  font-size: 18px;
  font-weight: bold;
}

.modal-body {
  padding: 20px;
}

.modal-footer {
  display: flex;
  border-top: 1px solid #eee;
}

.btn {
  flex: 1;
  height: 50px;
  line-height: 50px;
  text-align: center;
  background-color: #fff;
  border-radius: 0;
  font-size: 16px;
}

.btn::after {
  border: none;
}

.cancel {
  color: #666;
  border-right: 1px solid #eee;
}

.confirm {
  color: #7D6B91; /* 紫色调 */
  font-weight: bold;
}

.title {
  font-size: 20px;
  margin-bottom: 20px;
}

.training-time {
  font-size: 28rpx;
  color: #7D7D7D;
  margin-top: 10rpx;
}

.disabled-songbo {
  opacity: 0.5;
  filter: grayscale(50%);
} 